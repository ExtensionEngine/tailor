import authMutations from './mutation-types';

// TODO: Placeholders, replace with real actions
function loginRequest({ commit }) {
  commit(authMutations.LOGIN_REQUEST);
}

export default {
  loginRequest
};
