'use strict';

const processDigest = ({ dataValues }, revisions) => {
  console.log(dataValues);
  console.log('++++++++++++++++++');
  console.log(revisions);
};

module.exports = {
  processDigest
};
